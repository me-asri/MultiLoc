<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.MultiLoc" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimaryDark">@android:color/system_neutral1_900</item>

        <item name="actionBarTheme">@style/Base.Theme.MultiLoc.ActionBar</item>
    </style>

    <style name="Base.Theme.MultiLoc.ActionBar" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <!-- Change overflow icon color -->
        <item name="colorControlNormal">@android:color/system_accent1_500</item>
        <!-- Change drawable icon color -->
        <item name="iconTint">@android:color/system_accent1_500</item>
        <!-- Change title text color -->
        <item name="titleTextColor">@android:color/system_accent1_500</item>
    </style>
</resources>